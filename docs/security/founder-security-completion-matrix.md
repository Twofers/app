# Founder security completion matrix

This is the requirement-by-requirement evidence ledger for the unchecked items
in `docs/plans/founder-security-hardening-plan-2026-07-29.md`. It does not turn
an approval-gated task into a developer task and does not authorize production
or provider mutations.

| Plan requirement | Evidence already present | Evidence still required for completion | Owner / gate |
|---|---|---|---|
| Supabase Backups page and 7-day PITR decision | Timestamped CLI output reports `backups: null`, empty physical-backup data, and `pitr_enabled: false`; the signed-in dashboard confirms the Free plan includes no scheduled backups; independent backup tooling exists; founder approved a zero-added-cost posture on 2026-07-29 | PITR remains explicitly deferred until a 24-hour RPO becomes unacceptable; activate the separate free-tier backup | Founder decision recorded; no paid activation |
| Separate immutable backup account | S3-compatible Object Lock Compliance upload, re-read verification, encryption, checksum, heartbeat, failure hook, 900 MB per-archive ceiling, 9.5 GB aggregate ceiling, and 7-daily/3-monthly bootstrap retention | Separate free-tier Backblaze B2 account/bucket/application keys, lifecycle rules, and first successful locked upload | Founder creates account; developer can observe approved run |
| Secret values recovery | 103-name generated inventory; outside-Git encrypted vault creator/verifier; provider re-issue guidance | Founder populates real values/instructions without sharing them; verifies two offline copies | Founder credentials; joint drill |
| EAS/App signing recovery | Local filename inventory and signing-credential runbook | Interactive EAS export, fingerprint validation, encrypted offline copies | Founder interactive provider access |
| Quarterly restore drill and RPO/RTO | Structural database verifier; opt-in disposable DB restore; restored Storage/Auth/Function verifier; report output | Disposable project, backup object, throwaway credentials, re-minted test secret, measured first drill | Founder provisions/approves disposable resources; joint execution |
| Stripe root-of-trust controls | Signature and livemode checks; dispute event implementation; restricted read-key backup option | Founder MFA/recovery, payout alerts, restricted production keys, signing-secret rotation, Radar review | Founder/provider mutation |
| Apple Developer, Google Play/Cloud, Expo/EAS, founder Google, OpenAI/Gemini, Resend, Supabase/Vercel/registrar/Cloudflare controls | Per-provider checklist and recovery guidance | Interactive MFA, membership/session/token audits, recovery and spend/key-control evidence from each console | Founder/provider mutations |
| Supabase SSL/network/password/token controls | Migration parity; test-project SSL enforcement enabled and healthy; timestamped production snapshot proves SSL disabled plus `0.0.0.0/0` and `::/0` access. **Direct-client validation complete** (`direct-database-client-tls-validation-2026-07-29.md`): `db.<ref>` endpoints are IPv6-only/unreachable so all direct clients use the IPv4 pooler; the pooler refuses every cleartext startup on both projects and both ports; the Supabase CLI and a raw libpq-style client both complete authenticated round trips against the enforced test project. Two cautions recorded: pooler refusals are pooler behavior, not per-project proof, and the CLI ignores `sslmode` including `verify-full`. Backup/restore tooling now upgrades to `verify-full` when a CA is supplied and fails closed if it is unusable | Founder approval for production SSL; the `BACKUP_DB_ROOT_CERT_PEM` secret; network restrictions, password/token rotation | Founder/production control plane |
| Security Advisor and deep authenticated isolation probes | Initial signed-in/CLI scan found 2 errors and 171 warnings; anon could read 14 rows through untracked `public.deal_stats`; separately approved zero-cost migrations `20260824131000` through `20260824142000` are applied with parity confirmed; both view probes now return 401; anon Storage listings expose zero entries while sampled public assets remain HTTP 200; all 25 mutable-search-path findings, 26 trigger-only execution findings, 44 service-role-only execution findings, 24 non-public anonymous findings, six internal-helper authenticated findings, and one location-ownership helper finding are closed; the ownership helper's anon RPC route returns HTTP 401 and service-role RPC returns HTTP 200; Advisor reports 0 errors and 43 warnings; the deep catalog probe found zero TRUNCATE/REFERENCES/TRIGGER grants for `PUBLIC`, anon, or authenticated | **Both now supplied.** Named reachability triage completed: 39 of the 41 remaining function findings are individually dispositioned (6 required anon surfaces, 11 live RLS policy dependencies, 12 intentional signed-in RPCs, 3 closable and staged as `20260824144000`, 1 open product decision on `validate_business_invite`, 2 attributable to functions absent from the test project). Authenticated cross-tenant probes reran via `scripts/db-tests` (which mints its own throwaway users, so no founder identity was needed): 2e 13/13, 2f 9/9 after repairing two stale suites; 2c confirms one real defect | Approve `20260824144000`; decide `validate_business_invite`; approve `20260824143000` for the confirmed `business_locations` exposure |
| **Cross-tenant `business_locations` read exposure (new, 2026-07-29)** | Untracked policy `"Auth users can read business locations (pilot)"` (`SELECT TO public USING auth.uid() IS NOT NULL`) found in the live catalog and in no migration; owner A reads owner B's row; anon unaffected; sweep found no second instance; remediation staged as `20260824143000` with a source test | Approve/apply the migration; confirm production presence, which needs the founder-held production database password or a throwaway authenticated production identity | Founder approval / production credential |
| **Founder `unvmex2@hotmail.com` cannot obtain a production token (new, 2026-07-29)** | Password grant returns HTTP 500 `Database error querying schema`; other addresses return the normal 400, so production auth works in general and this is account-scoped | Admin read of that user's `auth` rows and identities — user-account enumeration against production, founder-gated | Founder |
| GoTrue hardening and demo account | Current login throttling and mandatory admin MFA code; **11-row change specification built** (`gotrue-hardening-specification-2026-07-29.md`) covering OTP expiry, session timebox/inactivity, anonymous sign-in, manual linking, CAPTCHA, password requirements, SMTP sender, and rate limits, each with rationale, cost, and failure mode; it also establishes that `supabase config push` must not be used and that CAPTCHA needs a client build first | Founder makes each dashboard change; developer ships the `captchaToken` client work before CAPTCHA is enabled; founder decision on demo account | Founder/auth production config |
| `admin.twoferapp.com` behind Cloudflare Access | Sealed same-origin cookie backend, strict CSP, no-store, proxy allowlist; the approved 28-function founder/rate-limit Edge activation is live and anonymous smoke-probed | DNS/Access policy, Vercel origin variables, website production deployment and founder/non-founder tests | Founder DNS/provider approval |
| Repository visibility and `main` protection | Pinned Actions, staged CodeQL/Dependabot, full-history secret scan, encrypted all-refs mirror tooling; timestamped GitHub snapshot shows PUBLIC, no protection/rulesets, and live Dependabot security updates disabled | Visibility decision; approved merge; enable live Dependabot security updates; ruleset including admins; required checks; separate mirror activation | Founder/GitHub mutation |
| **Code scanning and dependency PRs (new, 2026-07-31)** | Both controls are live since the PR #25 merge. All 81 first-scan CodeQL alerts triaged (`codeql-alert-triage-2026-07-31.md`); the two genuine findings in shipped Edge Function code fixed with regression tests; the other 79 dispositioned per rule and scoped out by committed config, not dashboard dismissals. Dependabot's two configuration faults — unconstrained Expo SDK packages and split `codeql-action` sub-actions — fixed in `.github/dependabot.yml` (`dependabot-pr-triage-2026-07-31.md`) | Founder merges this branch, closes the seven unmergeable PRs (#34, #33, #32, #31, #30, #28, #26) and optionally merges #27; separately, deploying the two fixed `_shared` consumers is a production approval | Founder merge/close; production deploy approval |
| GitHub token, Vercel promotion, DNSSEC/DMARC | Detailed operations checklist; timestamped DNS result shows no DS/DNSKEY and DMARC `p=none` | Token replacement, promotion/alert settings, registrar/DNSSEC activation evidence, staged DMARC observations | Founder/provider mutations |
| Developer machine | Filename-only secret inventory; elevated BitLocker command documented | Elevated BitLocker result; founder classification/vaulting of local secret/signing files; password-manager policy | Founder/local interactive access |
| `api.twoferapp.com` portability | Migration/deployment procedure documented | Explicitly deferred under the zero-cost bootstrap decision; reconsider only after revenue/data-loss risk justifies the add-on | Future founder cost decision |
| Simulated takeover exercise | Incident order, backup/restore and provider recovery runbooks, **plus an executable worksheet** (`takeover-recovery-exercise-2026-07-29.md`): preconditions with a tabletop fallback, scenario, six phases with today's commands, timing and RPO/RTO fields, corrective-action log, and the known gaps it will expose | Scheduled exercise with founder-controlled vault/providers; measured timeline and corrective-action record | Joint, after backup/provider activation |

No unchecked item is a repository-only implementation task that can be
truthfully completed without one of the external resources, identities,
provider-console decisions, or explicit production approvals listed above.

Reconfirmed 2026-07-29 (second pass). The four items that were previously
described as needing "developer triage" — direct-client SSL validation, the
authenticated cross-tenant probe rerun, the named Advisor reachability triage,
and the GoTrue specification — are now done, and the takeover exercise has its
developer half. What that work produced is two staged zero-cost migrations, one
CI secret request, and two new findings; all five are approval or credential
gated, not work gated.
