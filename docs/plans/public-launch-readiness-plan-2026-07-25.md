# Public-launch readiness plan — 2026-07-25

**Executor:** an AI agent (Opus) running in Claude Code on this repo (Phases A–B), then Dan (Phases C–E).
**Owner:** Dan. **This file is both the plan and the tracker** — update Status columns in place.
**Verdict this plan implements:** the code and backend are launch-grade, but do NOT build tonight. Five things stand between here and a safe public day 1: (1) a working-tree landmine that would poison the Android build, (2) a few small client fixes that must ride this rebuild or wait months for the next one, (3) console settings only Dan can check — one of which silently breaks Google sign-in on iOS only, (4) staged store verification, because Apple sign-in and Google-on-iOS have never been executed anywhere, and (5) a website deploy that carries the admin comp-grant UI (currently NOT live) and the privacy-policy update the new sign-in providers require.

---

## 0. Verified state this plan builds on (audited 2026-07-25, this session)

Everything below was re-verified today — not assumed from notes:

- **Local gates all green at HEAD `afeeedcd`:** `typecheck`, `typecheck:functions`, `lint` (0), `check:i18n-keys`, `gate:release-state`, `npm test` 282 files / 1984 tests. CI green on the last two pushes.
- **Backend parity is exact.** `supabase migration list --linked`: every local migration `local == remote` through `20260822180000` (all five merchant-onboarding fixes and both claim-links migrations are applied to prod). `supabase functions list`: **79 deployed == 79 local folders, all ACTIVE**, newest deploy 2026-07-25 (`ai-generate-ad-variants` v200). The two newest commits touch no backend files. **Nothing committed is undeployed on the backend.**
- **Production build profile is correct at HEAD:** `eas.json` production has `EXPO_PUBLIC_ENABLE_SOCIAL_AUTH="true"` with real (non-placeholder) Google web + iOS client IDs, no debug flags, mobile-billing flags off (intended), and the held poster-viewer-language flags correctly absent. `app.json`: version `1.0.1`, `ios.usesAppleSignIn: true`, both sign-in plugins, correct bundle/package ids. Committed `android/app/build.gradle` is production-correct. No `ios/` dir (EAS prebuilds iOS cleanly).
- **The 2026-07-24 rare-feature QA sweep** (docs/plans/pre-launch-rare-feature-qa-plan-2026-07-24.md) stands: zero open P0 anywhere; merchant-onboarding P0s (F-23/F-24) fixed **and applied to prod**; RLS denial 14/14 + smoke 7/7; all admin fns gated; delete-account proven end-to-end.
- **es/ko localization fixes are committed** (`80fb6a10`, `064f82a3`) and will ship with this rebuild.
- **Live website matches the repo** (asset `?v=` parity + the 07-23 LCP marker are live) **EXCEPT** `website/admin/trial-requests.js`: the live file (418 lines) predates commit `90f41f46` (473 lines) — **the admin comp-grant UI is committed but NOT deployed** (verified by diffing the live file against HEAD).
- **Auth email is on verified custom SMTP** (Resend, DKIM/SPF, docs/SMTP_SWAP_CHECKLIST.md) — but the Supabase auth email rate limit is **30/hour** (checklist §D), a real day-1 ceiling.
- **No crash-reporting SDK exists in the app** (no sentry/bugsnag/crashlytics in package.json). Day-1 visibility = store consoles + Supabase logs.
- Android Google sign-in matrix closed green on the S10 (2026-07-25), including the immutable-role race fix. **iOS is the untested half.**

## 1. Hard rules (inherited from CLAUDE.md — they all apply)

1. NO builds, store submissions, deploys (functions or website), migrations, pushes, or credential changes without Dan's explicit per-action approval. Phases C–E are Dan-gated by design.
2. AI poster/ad lock: before touching ANY file covered by `docs/ai-poster-core-lock.json` (including `CLAUDE.md`/`AGENTS.md` and `app/create/ai.tsx` — see B5), stop and get per-file approval, then update hashes and CHAIN `latestApprovalRef` ("… Prior ref: <old>").
3. Preserve all untracked artifacts and unrelated WIP. Never print secrets, tokens, QR/claim codes.
4. Client-only fixes: implement + validate locally; commit only when Dan approves. One scoped task at a time.
5. Validation after each Phase B item: `npm run typecheck && npm run lint && npm test`, plus `npm run check:i18n-keys` and `node scripts/check-i18n-defaultvalue-gaps.mjs` for anything touching copy. New user-facing strings go to en/es/ko, never `defaultValue`-only.

---

## Phase A — Working-tree hygiene (Opus, ~30 min, BEFORE anything else)

`eas.json` sets no `cli.requireCommit`, so **EAS uploads the dirty working tree as-is**. The tree must be clean-by-intention before any build.

| # | Task | Why | Status |
|---|------|-----|--------|
| A1 | `git restore android/app/build.gradle` and verify `git diff android/` is empty | The tree's uncommitted diff switches `applicationId`/`namespace` to `com.unvmex2.twoforone.dev` and **deletes the google-services plugin line** (dev-APK leftovers). Built as-is: wrong package (store rejects the AAB) and FCM/Google-services dead. Committed HEAD version is correct — restore, don't edit. | ☑ **DONE** 07-25. Restored; `git diff android/` empty; production `namespace`/`applicationId` and the google-services line confirmed back. Dirty copy backed up to the session scratchpad, not deleted. |
| A2 | ONE commit pairing `website/vercel.json` + `website/scripts/` (+ `docs/website-edit-checklist.md`, `scripts/e2e-smoke.js`) — Dan approves the commit | The uncommitted vercel.json sets `buildCommand: node scripts/check-i18n.mjs` while `website/scripts/` is untracked. Committed apart, **every future Vercel deploy fails at build**. e2e-smoke.js is referenced by root `package.json` `test:e2e` (verified passing locally today, en/es/ko). | ☑ **DONE** — commit `e37f0cb9`. Also carried the untracked `website/CLAUDE.md`. Verified before committing: `node website/scripts/check-i18n.mjs` passes (309 keys × 3 locales) and `npm run test:e2e` is green across en/es/ko. |
| A3 | Commit the 3 `eas.json` env lines (held poster-language flags) — they are on the **dev-apk-ai-studio profile only** (verified absent from production/apk) | Ends the dirty-file ambiguity; inert for production builds. If Dan prefers, stashing is acceptable — but decide, don't leave it dirty. | ☑ **DONE** — commit `3b5652c8`. Re-verified the 3 lines sit only under `dev-apk-ai-studio`. |
| A4 | Wire `node scripts/check-i18n-defaultvalue-gaps.mjs` into `.github/workflows/ci.yml` (next to `check:i18n-keys`) | The guard that catches the "53 English strings shown to es/ko users" class exists and is committed but NOT enforced — the class regresses silently without it. (Approval Queue #3, second half, from the QA plan.) | ☑ **DONE** — commit `a7cbde5b`. New "i18n defaultValue gaps" step runs between key parity and Test. |
| A5 | Add the untracked plan docs (`poster-item-language-mismatch-plan-2026-07-23.md`, `wallet-pass-go-live-runbook-2026-07-23.md`, this file) to the same or a docs commit | Trackers should ride the repo; they're referenced by device QA below. | ☑ **DONE** — commit `9906dd9b`. Plus `286e8334` for the `deno.lock` catch-up on the already-committed sign-in deps. **Working tree finished Phase A clean.** |

## Phase B — Client fixes that must ride THIS rebuild (Opus, ~half day)

These are all rebuild-gated: skip them now and they wait for the next store release.

> **Status 2026-07-25: all six done, validated, and UNCOMMITTED** (Phase B rule 4 — Dan approves the commit). 19 files: 15 modified, 4 new (`hooks/use-delete-account-flow.ts` + 3 test files).
> Gates run after the full set, all green: `typecheck` · `typecheck:functions` · `lint` (0) · `npm test` **285 files / 2001 tests** (was 282/1984) · `check:i18n-keys` · `check-i18n-defaultvalue-gaps` (baseline now empty) · `gate:release-state` · `gate:ai-poster-lock` (30/30) · `check:website-supabase` · `test:e2e` (en/es/ko).
> **No locked file was modified and no lock hash changed.** Two of the six items' premises turned out to be wrong on inspection — see B5 and B6.

| # | Task | Detail / acceptance | Status |
|---|------|---------------------|--------|
| B1 | **Delete-account reachability for business accounts with no approved application** (F-3 from the sign-in QA — P1 here) | The published privacy policy says "You can start account deletion inside the app under Account or Settings", and Apple guideline 5.1.1(v) requires it — but a business-role account with no approved application currently has **no path to delete-account at all** (device-verified 2026-07-25; Dan had to delete the test user from the dashboard). Investigate the no-application business landing state and give it a working path to Account → Delete (localized en/es/ko). Acceptance: a business user with (a) no application and (b) a pending application can complete in-app deletion; shopper path unchanged; tests added. | ☑ **DONE**. Root cause: `account` is in `BUSINESS_TABS` (lib/tab-mode-redirect.ts), so an incomplete business profile redirects **every** business tab — Account included — to `/business-setup`, which offered only Log out and Apply. Extracted the deletion flow to `hooks/use-delete-account-flow.ts` (two branded confirms → `deleteUserAccount()` → local sign-out → `/auth-landing`, website fallback on error) and rendered it on the pending-approval screen, which covers both `appStatus === "none"` and `pending`. Account now consumes the same hook — one implementation, not two. Copy already existed in en/es/ko. 7 tests in `lib/delete-account-reachability.test.ts`. **Not covered (deliberate, not a dead end):** an incomplete-profile merchant on the setup *form* can still finish the profile and reach Account. |
| B2 | F-10 remaining raw-error paths | `app/redemption-mode.tsx:252`, `app/(tabs)/redeem.tsx:163`, `lib/auth-error-messages.ts:79` — route through `translateKnownApiMessage` so es/ko users never see raw English errors. One-liners per the QA audit. | ☑ **DONE**, but the third was **not** a one-liner. Sites 1–2 were. Site 3 (`friendlyAuthMessage`'s `return raw`) also carried *useful* GoTrue text: masking everything would have turned "User already registered" into "Something went wrong", a real signup regression. Added an explicit branch → new `auth.errEmailAlreadyRegistered` (en/es/ko), then masked the rest via the translator. Net gain: infra strings with no auth branch (JWT expired, RLS) now map too. Password length and email format are rejected client-side, so they never reach here. |
| B3 | F-08 business-apply double-submit | Keep the submit button disabled through the redirect; map the 429 response to a localized message. | ☑ **DONE**. `finally` re-enabled the button unconditionally while the success path schedules a 1s delayed navigation — a full second of re-tappable Submit. Now only re-enables on failure. The 429 (`"Too many requests. Please try again later."`, surfaced verbatim by `invokeErrorMessage`) is mapped to new `apiErrors.rateLimitTryLater` in en/es/ko — done in the shared table, so every caller benefits. |
| B4 | F-09 "Release deal" confirmation | Wrap the single-tap forfeit in `useBrandedConfirm` (never `Alert.alert`). **Dan decision** — default = add it (a forfeit is not always re-claimable: sold-out / daily-cap). | ☑ decision (took the plan's documented default: added) + ☑ fix. `confirmReleaseWalletClaim` wraps the release in `useBrandedConfirm`, carrying the status and in-flight guards. Copy warns the claim may not be recoverable. **Reversible if Dan disagrees.** |
| B5 | (Optional — LOCKED FILE) F-16 es/ko revision label | en "Revision {{number}}" vs es "Ajuste" / ko "수정" — es/ko merchants can't tell revisions apart. The copy renders from `app/create/ai.tsx`, which **is covered by the AI poster lock** → needs Dan's per-file approval first; then fix + regenerate lock hash + chain `latestApprovalRef`. Skip cleanly if not approved. | ☑ **DONE — and it needed no approval.** The premise was wrong: `ai.tsx:4722` already passes `number`, and the missing placeholder was only in the es/ko **locale JSONs**, which no lock covers. Fixed there (`"Ajuste {{number}}"` / `"수정 {{number}}"`); `gate:ai-poster-lock` still reports 30/30 files unchanged. Also removed the now-obsolete `createAi.imageVersionRevision` entry from the `PLACEHOLDER_BASELINE` in `scripts/check-i18n-defaultvalue-gaps.mjs`, which had been baselined on that same wrong premise — so CI (per A4) now enforces it. |
| B6 | F-12 un-stale the website readiness gate | Update the 3 stale assertions in `scripts/check-website-supabase-readiness.js` (iOS store URL now live, `tryGetServiceRoleKey()` helper, current `?v=` values) so `check:website-supabase` is green **before** the Phase E deploy — a permanently red gate is how real regressions slip through. | ☑ **DONE — premise was stale.** All three were already fixed by commit `b7ddd0f5` ("Unstick the website readiness gate"), and `check:website-supabase` was **already green**. What remained rot-prone were two frozen `?v=` literals (`admin-login.js`, `claim.js`) — the exact anti-pattern this file's own comment documents as having broken the gate before. Replaced with `assertCacheBustedScript`, which requires *a* cache key; the hardening those versions stood for is already asserted behaviourally (opt-in `remember`, `setFormEnabled(false)`). Probed: accepts current and future versions, still fails on a missing key or missing script. |

## Phase C — Console/dashboard checks (DAN, ~30 min, before the builds)

None of these are code; every one is a launch-day failure mode if wrong.

| # | Check | What good looks like | Status |
|---|-------|----------------------|--------|
| C1 | **Supabase → Authentication → Providers → Google → Client IDs** | Contains BOTH client IDs, comma-separated: the web client ID AND the iOS client ID. iOS tokens carry the iOS client ID as audience — **if only the web ID is listed, Android keeps working and iOS Google sign-in fails with an audience error.** Cannot be checked from the CLI (sign-in plan Phase 4 box, still unchecked). | ☑ **DONE — confirmed by Dan 2026-07-25.** Not independently verifiable from this repo (no CLI or API surface exposes Supabase auth provider config), so this rests on Dan's dashboard check. Cross-reference used: `eas.json` production carries two **distinct** Google client IDs from the same project — web `991672961636-g49j35…` and iOS `991672961636-2aishd…` — and the Supabase field must list **both**, comma-separated. Still to be *proven* end-to-end by D3 (Apple/Google sign-in on TestFlight), since a wrong-but-present ID fails exactly like a missing one and only shows up on a real iOS device. |
| C2 | **Supabase → Authentication → Rate Limits → email send** (currently **30/hour**) + your **Resend plan** | Raise to ≥200/hour for launch week (confirmation emails are the signup gate). Resend free tier is 100 emails/day — if you're on free, upgrade or day-1 signups stall mid-afternoon. | ☑ **ACCEPTED AS-IS — Dan's explicit decision 2026-07-25**, not an oversight: he does not expect >30 signups/hour or >100/day at pilot scale, which is reasonable for a DFW launch. Left at 30/hour on the current Resend plan. **Binding constraint is actually Resend's 100/day, not the 30/hour**, over a full day. Low-cost to be wrong: raising either limit takes effect immediately with no deploy or rebuild. Detection is already covered — day-1 runbook check #4 watches Supabase Auth logs for `over_email_send_rate_limit` / 429s; if that fires, upgrade mid-day. |
| C3 | **Stripe Dashboard → Webhooks → prod endpoint → subscribed events** | Must include every type the deployed handler processes: `charge.dispute.created`, `charge.refunded`, `refund.created`, `checkout.session.completed`, `checkout.session.expired`, `customer.subscription.created/updated/deleted`, `customer.updated`, `invoice.paid`, `invoice.payment_failed` — **11 types**, not the 10 first listed here (`refund.created` is handled alongside `charge.refunded` via `isRefundWebhookEvent`). The dispute handler shipped later than the endpoint was configured — if the event isn't subscribed, chargeback auto-suspension silently never fires. | ☑ **FIXED 2026-07-25 — 11/11, verified against live.** Endpoint `we_1TxANO1k90plY1fzd4ycKx7g` now returns all 11 `enabled_events` with `"livemode": true`, `status: enabled`, URL unchanged, and a two-way set comparison against the handler's branches shows nothing missing and nothing subscribed-but-unhandled. **How it was found and fixed (worth knowing for next time):** the Workbench UI has **no way to edit an existing destination's event list** — "Edit destination" only edits URL/name/description, the `…` menu offers only disable/roll-secret/delete, the legacy `/webhooks/{id}` URL redirects into the same view, and the hosted Shell is **read-only in live mode** ("Switch to a sandbox to run API requests that create, modify, or delete"). It had to be done through the API. Note also that `stripe login` issues a **restricted key that can read but not write** webhook endpoints, so the write needed a separate restricted key with Webhook Endpoints → Write (created, used once, revoked). Original state for the record: 8 events, missing `charge.dispute.created`, `customer.updated`, `checkout.session.expired`. <br><br>_Prior status: ◐ CHECKED — GAP CONFIRMED, FIX NOT YET APPLIED._ Live mode has exactly **one** destination (`we_1TxANO1k90plY1fzd4ycKx7g` → `…supabase.co/functions/v1/stripe-webhook`, Active, error rate 0%). Mode confirmed live by the "Switch to a sandbox to set up a local listener" banner — the endpoint's *description* says "test mode" but that is a stale free-text label, not the mode. It subscribes to **8 of 11**. Missing: **`charge.dispute.created`** (add — silent chargeback hole, also open since the 2026-07-07 money & trust audit), **`customer.updated`** (add — profile changes never sync back), and `checkout.session.expired` (optional; `stripe-expire-pending-checkout` already polls Stripe for session status, so it is covered by a pull). Fix is Edit destination in the dashboard — no deploy, no code. **Loose end: `Import · 2`** — two legacy endpoints not migrated into the destinations view still function; confirm neither is a second live endpoint. |
| C4 | **Stranded-merchant census** (F-21 residual) — run in SQL editor:<br>`SELECT b.id, b.name, b.status, a.status AS application_status FROM businesses b JOIN business_applications a ON a.business_id = b.id WHERE b.status IN ('trialing','active','limited_trial') AND a.status NOT IN ('approved_not_activated','approved');` | Zero rows, or approve each returned application. **Standing operational rule:** approve → merchant signs in once → THEN comp/activate (first sign-in stamps the claim permanently). | ☐ |
| C5 | **AI spend headroom** | OpenAI prepaid balance topped up; Gemini quota OK; default cap is **30 AI generations per business per month** (`AI_MONTHLY_LIMIT` function env) — decide if that fits launch merchants, raise via secret if not. | ☐ |
| C6 | **EAS credentials sanity** (should all be no-ops since 1.0.0 shipped) | iOS: APNs key + distribution cert present in EAS. Android: keystore unchanged. `EXPO_PUBLIC_GOOGLE_MAPS_ANDROID_KEY` EAS secret still set (F-14: confirmed working on the live 1.0.0 build). | ☐ |
| C7 | **Reviewer demo account** still signs in (App Review notes reference it) | Demo credentials work on prod; demo deals render. | ☐ |

## Phase D — Build + staged verification (DAN-gated; agent assists with checklists)

Do NOT go straight from `eas build` to public release. The staging path below is the only place Apple sign-in, Google-on-iOS, and the Play-store-signed Google flow can be proven.

| # | Step | Notes | Status |
|---|------|-------|--------|
| D1 | EAS production builds (iOS + Android AAB) from `qa/poster-ad-quality` after Phases A–B land | `autoIncrement` handles build numbers (remote: iOS 27→28, Android 57→58). `expo.version` stays 1.0.1 (already bumped; `gate:release-state` green today). | ☐ |
| D2 | **Android:** upload AAB to Play **internal testing** track; install on the S10 **from Play** | This is the only build signed with the **Play App Signing** key — the known prod-only breakage mode for Google sign-in. Sideloaded/EAS-signed builds cannot prove this. | ☐ |
| D3 | **iOS: TestFlight matrix on Dan's iPhone** | Apple sign-in as shopper · Apple sign-in with **Hide My Email** as shopper (works) · Hide My Email attempting **business** (guard fires → local sign-out → retry path works) · Google sign-in both roles · email/password unchanged · **delete account** · core loop (browse → claim → redeem QR) · push received (favorite a business, publish from the S10). | ☐ |
| D4 | **S10 device matrix** (the deferred Phase-4 subset of the rare-feature QA plan) | D1 lifecycle incl. on-device delete · D3 favorites→publish→push routing · D5 create paths (edited headline survives publish; schedule cannot invert; AI cooldown countdown) · D6 redemption edge (repeat-restricted hides after redeem; redeemed toast; redemption-mode enter/exit) · D10 permission-denial matrix · spot-check ~10 new es/ko strings (F-05) + demo-offer copy in Spanish (F-17) · K9 spot-checks (logo upload, poster text edit, favorites dropdown, sort pills, QR live watch, site import). Record PASS/FAIL per line here. | ☐ |
| D5 | Store metadata for the new sign-in providers | App Privacy (iOS) + Data Safety (Play) updated for Google/Apple auth; App Review notes keep the demo account; release notes written. | ☐ |

## Phase E — Website + release (DAN-gated)

> **Status 2026-07-25: E1, E2, E4 done. E3 blocked on Phase D (no build exists yet).**
> The website is **deployed and live**. Beyond E1 as planned, Dan asked to retire
> "specials" in favour of "deals"; that swept 39 English strings plus the es/ko
> equivalents, so the site now matches the store listing ("Twofer: Local Deals on
> Demand") and the app's own vocabulary. Committed as `b3330138` (Phase B),
> `c40ad8b2` (website), `30ce0681` (docs) — **still not pushed**.

| # | Step | Notes | Status |
|---|------|-------|--------|
| E1 | Privacy policy: add Google sign-in and Sign in with Apple as login providers (en/es/ko via website i18n), bump `?v=`, follow `docs/website-edit-checklist.md` | Today the policy doesn't mention either provider; store reviewers and the data-safety forms will point at it. | ☑ **DONE + VERIFIED 07-25.** New "Signing in" section (`privacy.signInTitle` / `signInBody` / `signInRelay`) in en/es/ko, naming Google (iPhone+Android) and Apple (iPhone), what each provider sends (name, email, account id — never a password), that only name+email scopes are requested, and the Hide My Email relay behaviour incl. why a relay address can't claim a business. `privacy.providersBody` now names Google/Apple in all three. Effective date bumped Jul 1 → **Jul 25, 2026** (policy materially changed — **Dan should confirm that date**). `localization.js?v=` bumped `20260722-l2-feed` → `20260725-signin-privacy` across all **22** including pages (inventory re-grepped: single version, no page missed). Copy verified against the code, not assumed: `lib/social-auth.ts` requests Apple `FULL_NAME`+`EMAIL` with a nonce, Google uses default `openid/profile/email`, and `shouldBlockBusinessRelayEmail` enforces the relay/business rule. **Checklist run in full, all green:** `check:website-i18n` (379 keys × 3 locales) · `check:i18n` (312 referenced keys, up from 309) · `check:website-ui` (37 routes × 2 viewports) · `test:e2e` (en/es/ko) · `check:website-supabase`. **Visual pass done** on a local server: privacy page renders in all three locales with the section present and no raw keys, console clean in each, 375px shows no horizontal scroll and zero overflowing elements, and the served page carries `/localization.js?v=20260725-signin-privacy`. Scope guard: 23 files, of which 21 are the `?v=` one-liner — only `localization.js` and `privacy/index.html` carry content. Sitemap `<lastmod>` step is a no-op (the sitemap has no `<lastmod>` elements at all). |
| E2 | **Deploy the website** (gated) | Ships E1 AND the committed-but-not-live **admin comp-grant UI** (`90f41f46`) — without this deploy you cannot comp merchants from the console on launch day. After deploy: `E2E_BASE_URL=https://www.twoferapp.com npm run test:e2e` + `npm run check:website-supabase` (green after B6). | ☑ **DEPLOYED 2026-07-25** on Dan's explicit go. `npx vercel deploy --prod --yes` from `website/`; `readyState: READY`, `target: production`, deployment `dpl_5HjtgQtYvkYojjicA9nJ4QjjBW8H`. **Live-verified after:** homepage serves `localization.js?v=20260725-signin-deals`; title is "Twofer - Live Local Deals"; the privacy sign-in section is present; **the admin comp-grant UI is now live** (`approve_full_access` went 0 → 2 occurrences in the live `trial-requests.js`, which was the launch-day comping blocker); production `E2E_BASE_URL=https://www.twoferapp.com npm run test:e2e` green across en/es/ko; infra sweep 4×200 with both `.well-known` files served as `application/json`; live privacy page renders with a clean console. |
| E3 | Release: submit iOS 1.0.1 with **phased release ON**; promote the Play AAB with **staged rollout** (20% → 100%) | iOS review typically 1–2 days — schedule the public/marketing push only after BOTH stores are live, not the same day you submit. | ☐ **BLOCKED — no build exists.** D1 has not run, so there is no iOS build or Play AAB to submit or promote. Store submission is also Dan-only by CLAUDE.md ("App Store Connect and Play Console forms" are out of scope for agents). Phases C and D must complete first. |
| E4 | Day-1 monitoring cadence (no crash SDK on board — decision recorded below) | 2×/day minimum: Play Console vitals + App Store crash reports; Supabase Edge Function logs (any 5xx) + Auth logs (rate-limit hits); Resend delivery dashboard (bounces/suppressions); Stripe events; admin AI-spend panel; support@twoferapp.com inbox. | ☑ **DRAFTED** → `docs/plans/day-1-monitoring-runbook-2026-07-25.md`. Seven checks with what-good-looks-like and act-if thresholds, an escalation table, and the two reversible rollout levers (Play staged halt, iOS phased pause). Calls out the three silent failure modes by name: the 30/hr auth email ceiling, the iOS-only Google **audience** error from a missing iOS client ID (C1), and an unsubscribed `charge.dispute.created` (C3). Execution is Dan's — every surface is a console. |

## Post-launch queue (deliberately NOT blocking launch)

- Staged RLS migrations, apply with probes after each: F-13 `terms_acceptances` owner arm (SQL ready, QA plan Approval Queue #5) · F-19 `business_locations` owner-read (Approval Queue #8).
- F-22 narrow retry around `auth.admin.deleteUser` in delete-user-account (deploy-gated).
- F-04 timing-safe CRON_SECRET compare (deploy-gated).
- K8 military-brand image-gen re-test (now unblocked; costs 1 generation of quota).
- F-11 polish set (analytics timezone label, silent expired reset link, consumer-setup escape hatch, unchecked logo follow-up write).
- Android edge case: unconfirmed-password-account + Google sign-in (needs a fixture account).
- Crash reporting (Sentry) in 1.0.2 — adding a native module days before launch is worse than launching without.
- Native-speaker skim of the AI-produced es/ko translations (F-05 note).
- Merge `qa/poster-ad-quality` → `main` when Dan wants (approval-gated; main currently differs only by one docs commit).

## Decisions Dan must make explicitly

| # | Decision | Default recommendation |
|---|----------|------------------------|
| 1 | **Demo/QA content on day 1:** real shoppers will see demo deals in the feed (now correctly localized), and "The Colonel's Brew" QA business exists in prod. Keep as feed-filler while real merchants ramp, or hide? | Keep demo deals (empty feed is worse), retire QA-business leftovers. |
| 2 | F-09: confirm-before-release-deal | Yes, add it (B4). — **Taken as the documented default and implemented 07-25. Say so if you want it reverted; it is a self-contained change.** |
| 3 | F-16 locked-file approval for the es/ko revision label | Approve (small, real es/ko UX gap). — **MOOT: no approval needed.** The fix lives entirely in the es/ko locale JSONs, which the poster lock does not cover; `app/create/ai.tsx` was not touched and its hash is unchanged. See B5. |
| 4 | Launch without crash reporting | Accept for 1.0.1; add Sentry in 1.0.2; compensate with E4 cadence. |
| 5 | AI monthly cap (30/business) sizing for launch merchants | Keep 30 unless you expect heavy generators; it's an env change later. |
