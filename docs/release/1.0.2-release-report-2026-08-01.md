# Twofer 1.0.2 release report

Date: 2026-08-01

Release branch: `release/1.0.2`

Release tag: `v1.0.2` (the tag target is the authoritative release SHA)

Decision: internal-candidate GO; public-release NO-GO until exact-binary device QA and reviewer/store gates below pass

## Binary baseline and change inventory

- Public 1.0.1 source SHA: `5b7eead0fa16048968e3a116415ad1d7fca699fc` (resolved from paired EAS store-build metadata; no 1.0.1 tag exists).
- Android 1.0.1 EAS build: `be3de745-8593-4e8c-8204-c5c203b1aa0f`, version `1.0.1`, build/version code `59`.
- iOS 1.0.1 EAS build: `7729c128-994d-4d26-9b36-ac9acc026281`, version `1.0.1`, build `30`.
- Inventory scope: 119 commits and 290 changed paths from the baseline through the pre-freeze release branch. This is the accumulated, reviewed mobile train plus website, admin, security, database, and release-documentation work; no mobile subset was cherry-picked or reverted.
- Exact reproducible inventory: `git log --reverse --format="%H %s" 5b7eead0fa16048968e3a116415ad1d7fca699fc..v1.0.2`.
- Exact changed-path inventory: `git diff --name-status 5b7eead0fa16048968e3a116415ad1d7fca699fc..v1.0.2`.

The material mobile scope includes claim/redeem reliability, double-tap QR and Use Deal manual redemption, Google Wallet routing, same-item BOGO copy, imageless provider fallback, menu-promotion format choice, AI copy quality, account/session security, merchant onboarding and activation states, site import, localization, native wallet support, dependency updates, and the capability-dark iOS Checkout path.

## Production configuration

- App version: `1.0.2`; EAS remote version management and `autoIncrement` remain enabled.
- iOS bundle identifier and Android package: `com.unvmex2.twoforone`.
- Production app variant resolves as `production`; production EAS evaluation excludes `expo-dev-client`.
- Production Supabase host resolves as `kvodhiqhdqnptqovovia.supabase.co`; the production EAS environment contains the Supabase URL and anonymous-key variables.
- Google Maps configuration resolves as present. Google web and iOS auth client IDs are present in the production build profile.
- The only `EXPO_PUBLIC_*` production-profile change relative to the 1.0.1 SHA is `EXPO_PUBLIC_ENABLE_IOS_TRIAL_CHECKOUT: absent -> true`.
- Debug panel, debug boot logging, preview matching, AI Studio development publishing, and screenshot-only flags are absent from the production profile.
- Android remains an app bundle and its submission profile is pinned to Play `internal` testing.
- The EAS archive inspection confirmed `website`, `docs`, `supabase`, `store-assets`, `outdated`, `.github`, agent/editor folders, local QA folders, `key.txt`, and every `.env*` file contain no uploaded files.

## Checkout and Apple Wallet decisions

- Android contains no usable merchant Checkout/pricing/billing path. The client requires `Platform.OS === "ios"` and the dedicated flag before invoking Checkout.
- iOS accepts only an HTTPS `checkout.stripe.com` URL. Missing business IDs, server rejection, network failure, and URL-open failure stay on localized approval-email and Contact Support guidance.
- Production migration `20260824150000_ios_trial_checkout_kill_switch.sql` is applied and the linked database reports no pending migration. The migration creates/upserts `ios_trial_checkout` as `enabled = false` with `launch_state = off`.
- `stripe-create-checkout-session` is deployed with the explicit `native_ios` source and refuses that source while the server switch is OFF.
- The Checkout capability is disclosed in Apple review notes, the public privacy policy, and the public business terms. It is described as remotely disabled at launch.
- Production Apple pass signing configuration is present by secret name: pass certificate, private key, WWDR certificate, pass type identifier, and Apple team identifier. Values were not displayed.
- `NATIVE_WALLET_PASS_ENABLED` is present and its stored digest matches `true`.
- `wallet-pass-issue` and `wallet-pass-webservice` were redeployed from the release source. The issue endpoint boots and rejects anonymous requests with 401; the PassKit `/v1/log` route responds 200.
- A real iPhone save/presentation remains an exact-binary QA gate; configuration and endpoint health do not substitute for PassKit device validation.

## Automated and hosted gates

| Gate | Result |
| --- | --- |
| `npm run typecheck` | PASS |
| `npm run lint` | PASS |
| `npm test` | PASS: 320 files, 2,216 tests before final wallet/UI test additions; rerun on the tag is required below |
| `npm run typecheck:functions` | PASS: 163 Edge Function files |
| `npx expo-doctor` | PASS: 18/18 |
| `npm run gate:release-state` | PASS |
| `npm run gate:ai-poster-lock` | PASS |
| `npm run check:i18n-keys` | PASS |
| `npm run check:mobile-store-copy` | PASS |
| `npm run check:website-supabase` | PASS |
| Production migration dry-run after deployment | PASS: up to date |
| Hosted Checkout function boot smoke | PASS: expected HTTP 400 without a business id |
| Standard hosted Edge Function smoke | PASS |
| Authenticated production RLS smoke | BLOCKED: configured QA shopper credentials are stale/invalid; no secret was exposed |

All local gates must be rerun on a clean `v1.0.2` candidate immediately before the EAS builds. A failure changes this report back to NO-GO.

## Policy, store copy, and URLs

- Apple display title target: `Twofer: Live Local Deals`.
- Intended category alignment: Apple primary `Food & Drink`; Google Play `Food & Drink`.
- Apple and Google release notes cover the broad 1.0.2 scope and contain no empty bullet. Google copy contains no Android external-payment direction.
- Public URL set: privacy, terms, business terms, support, delete-account, Apple association, and Android asset links under `https://www.twoferapp.com/`.
- Public website deployment and response verification are recorded in the final release-operations section after deployment.

## Exact build and internal distribution

| Platform | EAS build ID | Store build number | Internal destination | Status |
| --- | --- | --- | --- | --- |
| Android | pending | pending | Google Play internal | pending clean tagged build |
| iOS | pending | pending | TestFlight | pending clean tagged build |

## Reviewer and exact-binary QA

These items are hard public-submission gates and cannot be inferred from source tests:

- Consumer reviewer account signs in, is claim-clean, and sees at least two claimable live offers outside physical Dallas location.
- Merchant reviewer account signs in with an active entitlement and exercises business tools without a purchase.
- Android real-device checks: merchant publish/AI/fallback/format flows; new manual redemption paths; conflict/repeat behavior; site import; Spanish/Korean strings; Google and email auth; Maps/location denial/ZIP; deletion; claim/release/redeem; push/deep links/support; Google Wallet save and pass-to-app route; no payment CTA or URL.
- iPhone checks: the corresponding publish, redemption, localization, auth, location, deletion, claim/release/redeem, push, universal-link, support, and Apple Wallet save/presentation paths.
- TestFlight Checkout with the switch OFF must yield no usable URL. A later temporary ON test must cover success, cancel, failure, double tap, background/return, and already-active, followed by recorded restoration to OFF.
- Storefront eligibility and Apple review-note/privacy accuracy are reconfirmed against the exact build.

Reviewer credentials must be entered only in the store console. They must never be committed to this report.

## Rollout, halt, and rollback

- Android: internal testing first, then staged production rollout; never immediate 100% availability.
- iOS: TestFlight first, then Apple phased release.
- Halt at the current rollout stage and pause Apple phased release if sign-in failures, crash rate, claim/redemption failures, or support volume regress against the 1.0.1 baseline.
- Monitor sign-in failures, merchant activation attempts, iOS Checkout failures if later enabled, first-deal publication, claims, redemptions, deletion, and support volume.
- For any iOS Checkout policy, review, or production issue: set `ios_trial_checkout` OFF immediately. If necessary, follow with a binary where the client flag is also false.

## Final release operations

- Release tag/SHA: pending candidate freeze.
- Website deployment: pending.
- Android EAS build and Play internal upload: pending.
- iOS EAS build and TestFlight upload: pending.
- App Store title/category/storefront/phased-release setup: pending authenticated console verification.
- Google Play category/staged-rollout setup: pending authenticated console verification.
- Public release approval: withheld until every reviewer and real-device item above has evidence.
